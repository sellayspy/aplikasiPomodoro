const Waktu = () => {
    return (
        <ClockContainer>
            <TimerText
      </ClockContainer>
  )
};

export default Waktu;
const ClockContainer = styled.div``;
const TimerText = styled.h3``;
